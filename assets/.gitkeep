# This folder holds system architecture images (e.g. architecture.png exported from diagrams/architecture.mmd).
# Placeholder so Git tracks the directory before images are added; safe to delete once real assets exist.
